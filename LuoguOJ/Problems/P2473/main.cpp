#include <bits/stdc++.h>
#define int long long

signed main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);
  int k, n;
  std::cin >> k >> n;
  std::vector<std::vector<int>> f(n + 1, std::vector<int>(1 << n));
  for (int i = 0; i < n; i++) {
    int p, x;
    std::cin >> p;
    while (std::cin >> x, x) {

    }
  }
  std::vector<std::vector<int>> f(k + 1, std::vector<int>(1 << n));
  return 0;
}