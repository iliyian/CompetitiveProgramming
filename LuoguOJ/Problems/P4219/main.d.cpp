#include <bits/stdc++.h>
#define int long long
// using namespace std;

signed main(signed argc, char **argv) {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  freopen("main.d.in", "w", stdout);

  std::mt19937 rd(std::random_device{}());

  int N = 1e5;
  int n = N, m = N - 1;

  return 0;
}