#include <bits/stdc++.h>
#define int long long

void solve() {
  char ch;
  std::cin >> ch;
  if (ch == 'A') {
    std::cout << "QWSXZ";
  }
  if (ch == 'B') {
    std::cout << "VGHN";
  }
  if (ch == 'C') {
    std::cout << "XDFV";
  }
  if (ch == 'D') {
    std::cout << "ERFCXS";
  }
  if (ch == 'E') {
    std::cout << "WSDFR";
  }
  if (ch == 'F') {
    std::cout << "RTGVCD";
  }
  if (ch == 'G') {
    std::cout << "TYHBVF";
  }
  if (ch == 'H') {
    std::cout << "YUJNBG";
  }
  if (ch == 'I') {
    std::cout << "UJKO";
  }
  if (ch == 'J') {
    std::cout << "UIKMNH";
  }
  if (ch == 'K') {
    std::cout << "IOLMJ";
  }
  if (ch == 'L') {
    std::cout << "KOP";
  }
  if (ch == 'M') {
    std::cout << "NJK";
  }
  if (ch == 'N') {
    std::cout << "BHJM";
  }
  if (ch == 'O') {
    std::cout << "IKLP";
  }
  if (ch == 'P') {
    std::cout << "OL";
  }
  if (ch == 'Q') {
    std::cout << "WA";
  }
  if (ch == 'R') {
    std::cout << "EDFT";
  }
  if (ch == 'S') {
    std::cout << "WEDXZA";
  }
  if (ch == 'T') {
    std::cout << "RFGY";
  }
  if (ch == 'U') {
    std::cout << "YHJI";
  }
  if (ch == 'V') {
    std::cout << "CFGB";
  }
  if (ch == 'W') {
    std::cout << "QASDE";
  }
  if (ch == 'X') {
    std::cout << "ZSDC";
  }
  if (ch == 'Y') {
    std::cout << "TGHU";
  }
  if (ch == 'Z') {
    std::cout << "ASX";
  }
  std::cout << '\n';
}

int32_t main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);
  int t = 1;
  // std::cin >> t;
  while (t--) {
    solve();
  }
  return 0;
}