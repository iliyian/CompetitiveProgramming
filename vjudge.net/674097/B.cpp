#include <bits/stdc++.h>
#define int long long

signed main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);
  int d, g;
  std::cin >> d >> g;
  std::vector<int> t(g + 1), f(g + 1), h(g + 1);
  std::vector<int> ;

  auto dfs = [&](auto self, int u) {
    
  };

  for (int i = 1; i <= g; i++) {
    std::cin >> t[i] >> f[i] >> h[i];
  }
  return 0;
}