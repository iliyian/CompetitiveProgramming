#include <bits/stdc++.h>

int main() {
  freopen("B.in", "w", stdout);
  int n = 4e6;
  std::cout << n << '\n';
  for (int i = 1; i <= n / 2; i++) {
    std::cout << 1 << ' ';
  }
  std::cout << '\n';
  for (int i = 1; i <= n / 2; i++) {
    std::cout << 1 << ' ';
  }
  std::cout << '\n';
  return 0;
}