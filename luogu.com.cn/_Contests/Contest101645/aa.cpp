#include <bits/stdc++.h>
using namespace std;

int a[100001], x[100001];

struct State {
  int pos, cnt, t;
};

int main() {
  int n, t, f;
  cin >> n >> t >> f;
  for (int i = 0; i < n; i++) {
    cin >> x[i] >> a[i];
  }
  queue<State> q;
  while (!q.empty()) {
    State st = q.front(); q.pop();
    for (int i = 0; i < )
  }
}