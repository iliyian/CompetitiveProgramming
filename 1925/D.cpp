#include <bits/stdc++.h>
#define mod int(1e9+7)
using namespace std;

// p mod m*q=x

void solve() {
  int n, m, k;
  cin >> n >> m >> k;
  for (int i = 0; i < m; i++) {
    int a, b, f;
    cin >> a >> b >> f;

  }
}

int main() {
  ios::sync_with_stdio(false); cin.tie(0);
  freopen("D.in", "r", stdin);
  freopen("D.out", "w", stdout);
  int _; cin >> _;
  while (_--) solve();
}