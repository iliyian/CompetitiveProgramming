#include <bits/stdc++.h>
#define int long long


signed main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);
  int t = 1000;
  std::cout << t << '\n';
  for (int i = 1; i <= t; i++) {
    std::cout << i << '\n';
  }
  return 0;
}