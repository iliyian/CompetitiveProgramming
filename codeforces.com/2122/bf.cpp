#include <bits/stdc++.h>
#define int long long

void solve() {
  int n;
  std::cin >> n;
  std::vector<std::array<int, 2>> a(n + 1);
  std::set<std::array<int, 2>> sx, sy;
  for (int i = 1; i <= n; i++) {
    std::cin >> a[i][0] >> a[i][1];
  }
  for (int i = 1; i <= n; i++) {
    for (in)
  }
}

int32_t main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  int t = 1;
  std::cin >> t;

  while (t--) {
    solve();
  }

  return 0;
}