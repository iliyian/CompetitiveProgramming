#include <bits/stdc++.h>
#define int long long
// using namespace std;

std::vector<std::vector<std::vector<int>>> f(20, std::vector<std::vector<int>>(2521, std::vector<int>()))

int work(int x) {

}

void solve() {
  int l, r;
  std::cin >> l >> r;
  std::cout << work(r) - work(l - 1) << '\n';
}

signed main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  for (int i = 1; i <= 10; i++) {
    f[i][i][i] = 1;
  }
  for (int i = 2; i <= 19; i++) {
    for (int j = 0; j <= )
  }

  int _;
  std::cin >> _;

  while (_--) {
    solve();
  }

  return 0;
}