#include <bits/stdc++.h>
#define int long long

void solve() {
  int n;
  std::cin >> n;
  std::vector<double> a(n + 1);
  for (int i = 1; i <= n; i++) {
    std::cin >> a[i];
  }
  
  for (int i = 1; i <= n; i++) {

  }
}

signed main() {
#ifndef ONLINE_JUDGE
	freopen("C.in", "r", stdin);
#endif
	std::cin.tie(0)->sync_with_stdio(false);
	int t = 1;
//	std::cin >> t;
	while (t--) {
		solve();
	}
	return 0;
}
